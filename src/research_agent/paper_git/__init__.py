"""
P36: Paper-git — Version Control for Research

Git-like version management for research documents:
- Snapshot/version management with author, message, timestamps
- LaTeX-aware diff engine (word-level, section-aware)
- Branch/merge with 3-way merge and conflict markers
- PR-style review workflow (comments, approvals, merges)
- Checkpoint restore from any version
"""
